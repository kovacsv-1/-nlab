using UnityEngine;

public class GJKRaycast : MonoBehaviour
{
    //more shapecast/conservative advancement
    //calculates signed sum of meshes and the casts a ray from origin towards the signed sum of the two's velocities (currently only player's velocity) to get contact points and normals
}
